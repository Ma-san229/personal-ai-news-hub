# Personal AI News Hub — 運用手順

## 初期状態

このプロジェクトは、ニュースを「AI整体師向け」と「エンジニア向け」の二つのカテゴリで扱います。初期ソースとしてAI整体師とponkotsu.devの設定を登録しています。ponkotsu.devは確認済みのAtomフィードを有効化済みです。AI整体師のYouTubeチャンネルから導出したフィードURLはHTTP 404となったため、正しいRSSまたはAtom URLが分かり次第、「フィード管理」画面で追加・有効化してください。

| 初期ソース | カテゴリ | 状態 | 対応 |
| --- | --- | --- | --- |
| AI整体師 | AI整体師向け | 停止中 | 正式なRSS / Atom URLが判明後にURLを登録・有効化する |
| Engineer's Digest（ponkotsu.dev） | エンジニア向け | 有効 | Atomフィード `https://www.ponkotsu.dev/feed` を定期取得する |

## 日常運用

「ニュース一覧」の**最新記事を取得**を押すと、有効なフィードを即時に取得します。記事はURLのハッシュで重複排除されるため、同じ記事は再登録されません。記事カードの丸い既読ボタン、または「元記事を開く」操作により、個人ごとの既読状態を管理します。

フィードを追加する際は、公開されたHTTPまたはHTTPSのRSS / Atom URLを入力してください。ローカルネットワーク向けURLやプライベートIPアドレスは安全のため拒否されます。フィードを削除しても、すでに取得済みの記事はライブラリに残ります。

## noteを情報源にする

noteはクリエイター・マガジンごとのRSSを公開しているため、このアプリの「フィードを追加」からエンジニア向けソースとして登録できます。読みたいクリエイターまたはマガジンのページでRSSリンクを取得し、そのURLを貼り付けてください。通常のRSSでは記事冒頭の一部が配信されます。note proの独自ドメインでは、設定により全文RSSとハッシュタグによる配信対象の絞り込みも利用できます。[1]

## 定期取得の有効化

定期取得は、公開済みのサイトに対してのみ有効化できます。公開後に「フィード管理」を開き、定期取得のcron式と有効・無効を設定してください。cron式は**UTCの6項目形式**で指定します。たとえば `0 0 */6 * * *` は6時間ごとの実行を表します。

定期実行は `/api/scheduled/news-refresh` を呼び出します。このエンドポイントはスケジュール実行者として認証されたリクエストだけを受け付け、設定済みのジョブIDと照合するため、繰り返し実行されても安全に動作します。実行記録はフィードごとの最終取得時刻・状態、および定期実行設定に保存されます。

## GitHub Actionsについて

本プロジェクトでは、アプリケーションの定期実行機能を標準経路として採用しています。GitHubへコードをエクスポートする場合は、GitHub Actionsを**型検査・ユニットテスト・ビルド検証**に使う構成が適しています。RSSをDBへ書き込む定期実行までGitHub Actionsに担わせる場合は、公開APIの認証方式を別途設計し、シークレットをGitHub側に登録してください。二重取得を避けるため、定期取得の実行場所はどちらか一方に統一してください。

## 参照情報

[1] [noteヘルプセンター「記事の全文をRSSで受け取る設定」](https://www.help-note.com/hc/ja/articles/900001001246-%E8%A8%98%E4%BA%8B%E3%81%AE%E5%85%A8%E6%96%87%E3%82%92RSS%E3%81%A7%E5%8F%97%E3%81%91%E5%8F%96%E3%82%8B%E8%A8%AD%E5%AE%9A)
